{
    "version": "2.0.0",
    "tasks": [
        {
            // 调试前，先构建一遍
            "label": "build",
            "type": "shell",
            "command": "cargo build",
            // 可能需要给 cargo build 赋予参数，
            "args": [],
            "options": {
                "cwd": "${fileDirname}"
            }
        },
    ],
}